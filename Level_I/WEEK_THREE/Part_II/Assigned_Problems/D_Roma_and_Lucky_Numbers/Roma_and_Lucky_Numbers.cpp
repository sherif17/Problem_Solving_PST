#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, k;
    cin >> n >> k;
    int arr[100];

    for (int i = 0; i < n; i++)
        cin >> arr[i];
        int count=0;
        while(arr[i]--){
            

        }

    return 0;
}